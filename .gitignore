# ── R ─────────────────────────────────────────────────────────────────────────
.Rhistory
.RData
.Ruserdata
.Rproj.user/
*.Rproj
*.Rcheck/
*.tar.gz

# Pacotes instalados localmente (renv / pak)
renv/library/
renv/local/
renv/cellar/
renv/lock/          # manter se usar renv; remova esta linha nesse caso
.Rprofile           # idem

# ── Quarto ────────────────────────────────────────────────────────────────────
_site/              # HTML renderizado — gerado em build
_freeze/            # cache de execução (freeze: auto)
.quarto/            # cache interno do Quarto
*.knit.md
*.utf8.md

# Arquivos temporários de render
*_files/

# ── Saída gerada ──────────────────────────────────────────────────────────────
output/             # PNGs exportados por salvar_grafico()

# ── Sistema operacional ───────────────────────────────────────────────────────
.DS_Store
Thumbs.db
Desktop.ini

# ── Temporários deste projeto ─────────────────────────────────────────────────
tmp_rpath.qmd
tmp_rpath.html
tmp_rpath_files/
instalar_pacotes.R
